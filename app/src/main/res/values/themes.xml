<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/premium_accent</item>
        <item name="colorPrimaryVariant">@color/premium_accent_dark</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/premium_dark</item>
        <item name="android:colorBackground">@color/premium_dark</item>
        <item name="colorSurface">@color/premium_card_bg</item>
        <item name="colorOnSurface">@color/premium_text_primary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/premium_dark</item>

        <!-- Bottom Navigation style -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="backgroundTint">@color/premium_card_bg</item>
        <item name="itemIconTint">@color/premium_accent</item>
        <item name="itemTextColor">@color/premium_text_secondary</item>
    </style>
</resources>